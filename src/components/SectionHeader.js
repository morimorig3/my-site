const SectionHeader = ({ children }) => (
  <header className="mb-4 sm:mb-8">
    <h2 className="text-center font-bold text-xl text-slate-800">{children}</h2>
  </header>
);

export default SectionHeader;
